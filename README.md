# Vertical Tab Keyboard Toggle
Toggle Firefox vertical tabs with `F1`.

### Change Shortcut
Go to `about:addons` > Gear Icon > Manage Extension Shortcuts.

### Compress for upload
Run

```bash
zip -r -X vertical-tab-keyboard-toggle.zip manifest.json background.js icon.svg
```

### License
MIT